수행과제

- import numpy
- 각각 arr1, arr2, arr3 과 같이 ndarray 타입
- parts(=ndarray) = 합쳐진 3개의 배열 생성 : 평균값 구하기
- 평균값이 50 보다 작은 값 -> parts_to_work_on.csv
- 파일로 저장하는 부분에는 반드시 예외처리가 되어 있어야 한다.

보너스 과제
parts_to_work_on.csv를 읽어서 parts2라는 ndarray에 저장한다.

## parts2의 내용을 기반으로 전치행렬을 구하고 그 내용을 parts3에 저장하고 출력한다.

- ndarray : 배열 안의 모든 데이터 같은 타입 -> 연산이 빠름
- CSV나 TXT 파일에서 읽어오면, NumPy가 자동으로 ndarray 타입으로 생성 print(type(arr1))
- : (콜론 하나만 있음) $\rightarrow$ "처음부터 끝까지 전부(모든 행)" 선택, (쉼표) $\rightarrow$ 행과 열 구분을 위한 기호0 (숫자 0) $\rightarrow$ "0번째 열(첫 번째 세로줄)" 선택
